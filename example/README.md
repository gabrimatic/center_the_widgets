# CenterTheWidgets Example

This example shows a mobile-first Flutter screen running inside a constrained
center surface on large displays.

Run it locally:

```bash
cd example
flutter run -d chrome
```

The orange content deliberately asks for a very wide layout. With
`CenterTheWidgets`, the app keeps a readable mobile width while the surrounding
area uses the configured background color.
